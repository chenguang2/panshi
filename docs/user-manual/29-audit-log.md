# 29. 审计日志

> 本章场景：谁在什么时候改了什么？【审计日志】集中记录平台全部增删改操作（集群/路由/上游/节点/用户/数据库管理等），供管理员追溯与合规检查。

## 29.1 页面入口

点击左侧侧边栏：【系统管理】→【审计日志】。

> - 仅授权用户可见：管理员默认可见；普通用户可在【用户管理】中授予「审计日志」权限。
> - 若菜单不可见：回[第 1 章 功能开关前置检查](01-login.md)核对 `audit_log` 开关。

📷 截图待补充：审计日志列表页全景

## 29.2 列表与筛选

列表默认按时间倒序展示最近 20 条，每行包含：**时间、用户、操作、资源、资源ID、详情、IP**。

筛选条件（下拉选项自动从历史数据加载）：

| 筛选项 | 说明 |
| --- | --- |
| 用户 | 按操作人过滤 |
| 操作 | 按动作过滤（如 route_create、cluster_delete、db_migration_migrate） |
| 资源 | 按资源类型过滤（route/cluster/user/...） |
| 时间范围 | 默认最近 7 天；多条件之间为「且」关系 |

操作命名规则：`{资源}_{动作}`，如 `route_create`、`upstream_update`、`route_batch_delete`。

## 29.3 详情抽屉

点击「资源」列可跳转对应页面；点击行打开详情抽屉：

- 完整 detail 内容，长内容支持滚动；
- 【复制】按钮一键复制详情文本。

更新类操作的 detail 包含**变更前后对比**，例如：`更新路由 r14: uri 从 '/old' 变更为 '/new'`。

## 29.4 导出

- 【导出 CSV】/【导出 Excel】：导出当前筛选结果；
- 大数据量（超过 5000 行）由后端生成文件后下载，避免页面卡顿。

## 29.5 常见问题

| 现象 | 处理 |
| --- | --- |
| 菜单不可见 | 核对 `audit_log` 开关（第 1 章）与用户「审计日志」权限 |
| 失败的操作没有记录 | 设计如此：失败的请求（未提交）不产生审计记录 |
| 批量操作只有一条记录 | 设计如此：批量操作合并为一条汇总，detail 中含全部 ID |

下一步：[附录A 修改 hosts](附录A-修改hosts.md) / 返回 [目录](README.md)
