security_restrict_redis


redis端卡控


key_prefix
string，默认：O-
查询redis时添加的前缀

key
string/array，默认："${username}"
指定需要检查的数据来源，如：${username}，

bypass_missing_key
boolean，默认：true
如果指定的key不存在，是否不进行卡控

risk_expr
array，默认：[["redis.1", "==", "0"]]
验证数据是否命中

status
integer，默认值，403
拦截后，响应的状态码

message
string，
拦截后，响应的信息


redis_dc
string，
指定使用redis_conf中配置的其中一组服务器


redis_conf
object，
配置redis相关属性：

redis_conf.CLUSTER_xxxx
array
配置服务器列表，组名为xxxx，可配置一组或多组数据，例：
"CLUSTER_TEST" : [ [ "10.1.1.1", 6379 ], [ "10.1.1.2", 6379 ] ]
"CLUSTER_ABCD" : [ [ "10.1.1.1", 6379 ], [ "10.1.1.2", 6379 ] ]

redis_conf.MODE
string，["redis", "rediscluster"]，默认值，"redis"
redis类型

redis_conf.AUTH
string，
redis密钥

redis_conf.DATABASE
integer，默认值，0
redis使用的db

redis_conf.TIMEOUT
integer，默认值，1，单位s
连接redis的超时时间


说明：
可以同时配置插件数据属性和配置属性，或者配置其中之一，
如果都配置了，按优先级 配置属性 > 数据属性，使用优先级较高的属性




事例：




