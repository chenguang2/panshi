security_restrict_ip


IP卡控，通过黑白名单限制IP访问


blacklist
array[string]
黑名单列表，匹配IP是否在列表中，在其中的将被拦截。

whitelist
array[string]
白名单列表，匹配IP是否在列表中，不在其中的将被拦截。

key
string，默认：${remote_addr}
指定IP的来源，如：${remote_addr}，

bypass_missing_key
boolean，默认：true
如果指定的key不存在，是否不进行卡控

bypass_error_key
boolean，默认：true
如果指定的key异常，是否不进行卡控

status
integer，默认值，403
拦截后，响应的状态码

message
string，
拦截后，响应的信息


说明：
可以同时配置插件数据属性和配置属性，或者配置其中之一，
如果都配置了，按优先级 配置属性 > 数据属性，使用优先级较高的属性
正常情况下，黑白名单应当仅配置其中一个规则
如果同时配置了黑白名单，黑白名单不能有交集，优先按黑名单来处理
不使用白名单规则时，应当不配置白名单列表，
如果配置白名单列表为空数组，将限制所有ip访问


事例：




