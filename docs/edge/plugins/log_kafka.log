log_kafka

将请求信息按指定格式推送到kafka中



logs
array[string] 或 strings，默认："cluster_test"
推送哪个日志格式到服务器，一个或多个，如果要自定义日志格式，需要在对应的插件数据中配置
例："cluster_test"。

topic
string，
推送到kafka的topic

key
string，
推送到kafka的key


include_req_body
boolean，默认值，false
日志中是否会使用到请求体

include_req_body_expr
array[array]
满足该表达式的请求，才会用到请求体，需要开启include_req_body



插件数据属性：
logs
object，键值为string，内容为格式串或数组，默认：{ "cluster_test" : {"formats":["${req_start_time}", ...]} }
或 array[object], 例如：[{"cluster":"cluster_test", "formats":["${req_start_time}", ...]}]
需要记录的文件日志，可以配置一个或多个
logs中，每个元素都是object，属性包括：
cluster：string，日志记录的文件名，第一种格式可以省略该项
formats：string或array[string]，日志文件的格式，例："${remote_addrs},${host},${status}"
formats_sep：string，如果日志格式使用数组，则使用该字符拼接各项日志

topic
string，默认：testtopic
推送到kafka的topic

key
string，默认：${ngx_now}
推送到kafka的key

broker_list
string，默认：[ [ "host" : "127.0.0.1", "port" : 9092 ] ]
kafka服务器列表


说明：
如果没有添加插件数据，则只有cluster_test日志格式有效，格式为默认的简单格式






事例：





