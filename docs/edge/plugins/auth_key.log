auth_key




Router 端配置属性:

header
string，默认值，apikey
设置apikey从 header 中指定字段获取。
例：该值指定为xapikey时，apikey从Header["xapikey"]中获取

query
string，默认值，apikey
设置apikey从请求参数中获取key，
例：该值指定为xapikey时，apikey从url?xapikey=xxx，的args["xapikey"]获取

hide_credentials
bool，默认值，false
设置为 false 时，将透传给 Upstream。
设置为 true 时，将清除掉对应的信息，不传递给Upstream。

说明：
如果header，query都配置了，按优先级 header > query，取优先级较高的


Consumer 端配置属性:

key
string
为每个 Consumer 配置的 key，这个key应该是不相同的。
如果多个 Consumer 使用了相同的 key，将会出现请求匹配异常。




事例：

添加插件auth_key
curl -L -X PUT 'http://127.0.0.1:9990/edge/admin/routes/3001' -H 'X-API-KEY: f9357106bff442f89d4de7169c37c61e' --data-raw '{
    "uri": "/hello1",
    "plugins": {
        "auth_key": {}
    },
    "upstream": {
        "nodes": {
            "127.0.0.1:8111": 1
        },
        "type": "roundrobin"
    }
}'

新增consumer
curl -L -X PUT 'http://127.0.0.1:9990/edge/admin/consumers' -H 'X-API-KEY: f9357106bff442f89d4de7169c37c61e' -H 'Content-Type: application/json' --data-raw '{
    "username": "CAtest_auth_key",
    "plugins": {
        "auth_key": {
            "key": "xxxxyyyyzzzz"
        }
    }
}'

测试请求
curl -L -X GET 'http://127.0.0.1:9980/hello1' -H 'apikey: xxxxyyyyzzzz'







