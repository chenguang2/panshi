- name: init directory
  raw: "source /etc/profile; {{ item.1 }}/bin/manager install uap-edge;"
  loop: "{{ ips.split(',') | zip(prefix.split(',')) | list }}"
  when: inventory_hostname == item.0
  tags:
    - install_edge
