## 1. 后端修复

- [x] 1.1 `global_rules.py`：cluster_name 回退到 name
- [x] 1.2 `plugin_configs.py`：cluster_name 回退到 name
- [x] 1.3 `static_resources.py`：cluster_name 回退到 name

## 2. 前端顶栏

- [x] 2.1 `GlobalRuleList.vue` — 添加 `.gr-card-topbar` + CSS
- [x] 2.2 `PluginConfigList.vue` — 添加 `.pc-card-topbar` + CSS
- [x] 2.3 `StaticResourceList.vue` — 添加 `.sr-card-topbar` + CSS
- [x] 2.4 `PluginMetadataList.vue` — 添加 `.pml-card-topbar` + CSS（顶栏方案）

## 3. 验证

- [x] 3.1 前端构建通过
- [x] 3.2 前端测试通过
