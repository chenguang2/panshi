# Tasks: 节点任务 — 分发文件

## Group 1: 后端基础设施

- [x] 1.1 新增文件分发上传配置：文件大小限制可调下拉框（默认 10MB），无扩展名白名单
- [x] 1.2 文件分发上传端点：原样字节存储，不做编码检测/转换（独立于脚本上传端点）

## Group 2: 后端 API

- [x] 2.1 新增文件分发上传端点 `POST /node-tasks/upload-distribute-file`
- [x] 2.2 修改文件预览端点：二进制文件返回 `preview_available: false`

## Group 3: 后端执行引擎

- [x] 3.1 在 `_execute_node` 中新增 `distribute_file` 任务类型分支（仅创建 node_task_record，不执行）
- [x] 3.2 新增任务级执行路径：一次性调用 `run_playbook`（tag = `edge_master_copy_to_slaves`），传入所有节点 IP
- [x] 3.3 任务创建时将文件从 `task-scripts/temp/` 迁移到 `task-scripts/{task_id}/`
- [x] 3.4 解析 ansible 输出，根据每个节点的 exit code 更新对应的 `node_task_record`
- [x] 3.5 默认 timeout = 300s
- [x] 3.6 destpath 后端自动补尾 `/`

## Group 4: 前端 API

- [x] 4.1 新增文件分发上传函数（独立于脚本上传）
- [x] 4.2 复用预览函数：二进制文件处理

## Group 5: 前端 UI

- [x] 5.1 NodeTaskCenter 新增「分发文件」操作类型选项
- [x] 5.2 文件上传区域（独立于脚本上传，支持任意文件类型）
- [x] 5.3 文件大小限制可调下拉框（默认 10MB）
- [x] 5.4 目标目录路径输入框（必填，提示"请输入目标目录路径"）
- [x] 5.5 二进制文件预览提示 / 文本文件预览
- [x] 5.6 提交时组装 `distribute_file` 类型的 task_type + params
- [x] 5.7 补齐已上传文件列表/删除 UI（脚本上传和分发文件共享，修复脚本上传的遗留缺失）
  <!-- 2026-09-12 补正：此前勾选失实——仅"换文件时删旧上传"落地，列表 UI 缺失。
       现已补齐：入口为任务中心页级「已上传文件」按钮（工具栏，常驻可达——
       初版放在创建弹窗内，任务执行后入口即消失，已改）+ 独立弹窗（脚本/分发
       徽章、大小、上传时间、删除确认）；范围仅 temp/ 待用上传，已迁移文件归
       任务所有随任务清理（不做跨任务文件管理）。连带修复 delete 端点只认
       {id}.sh 导致分发文件（{id} 无后缀）删除 404 静默泄漏的 bug；list 增加
       kind/uploaded_at。 -->

## Group 6: 测试

- [x] 6.1 后端上传 API 测试（任意文件类型、原样字节存储验证）
- [x] 6.2 后端分发执行测试（mock ansible，验证多节点统一执行 + 结果解析）
- [x] 6.3 前端组件测试（文件上传、目标路径、提交参数）
