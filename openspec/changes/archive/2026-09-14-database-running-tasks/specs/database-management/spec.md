## ADDED Requirements

### Requirement: 当前任务展示
数据库管理页面 SHALL 展示当前正在执行的任务（迁移 + 节点任务），帮助管理员了解写操作被拒绝的原因。

#### Scenario: 任务列表与写操作拒绝关联
- **WHEN** 管理员遇到写操作返回 503"正在迁移数据库"提示
- **THEN** 数据库管理页面的"当前任务"卡片 SHALL 显示迁移任务正在执行，提供上下文信息
