## 1. Template Restructuring

- [x] 1.1 Change `ConfigDiff.vue` template: wrap each `diff-row` in a container div and add inline `v-if` for expanded fields
- [x] 1.2 Remove the separate `v-for="item in expandedItemsList"` section
- [x] 1.3 Remove `expandedItemsList` computed property and unused `computed` import
